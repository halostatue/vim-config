scriptencoding utf-8

if hs#plug#in('vim-operator-replace')
  map gr <Plug>(operator-replace)
endif

if hs#plug#in('vim-niceblock')
  " xmap I <Plug>(niceblock-I)
  " xmap A <Plug>(niceblock-A)
endif
